spring:
  application:
    name: ms-zipkin
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
    addresses: amqp://localhost:5672
    username: guest
    password: guest

encrypt:
  fail-on-error: false
