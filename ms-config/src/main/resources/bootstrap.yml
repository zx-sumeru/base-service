spring:
  application:
    name: ms-config

  cloud:
    config:
      server:
        git:
          uri:
          username:
          password:

  rabbitmq:
    addresses:
    username:
    password: